<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@color/surface">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="2dp"
        android:paddingBottom="2dp"
        android:gravity="center_vertical">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/categoryLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="14sp"
                android:maxLines="1"
                android:ellipsize="end"
                android:textColor="@color/text_dark" />

            <!--
                Kept in the layout but never shown. The hint ("Sum of 1s") is what makes a row
                two lines tall, and two lines times seventeen rows is what forces scrolling on a
                screen this short. The view stays so the adapter can still find it — a missing id
                would be a crash rather than a tidier layout — and the rule it explains is one you
                only need told once anyway.
            -->
            <TextView
                android:id="@+id/categoryHint"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="11.5sp"
                android:textColor="@color/text_muted"
                android:visibility="gone" />

        </LinearLayout>

        <!-- One cell per scorecard, so a multi-card game reads as columns on a single sheet
             rather than something you flip between. -->
        <LinearLayout
            android:id="@+id/scoreCells"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal" />

    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/row_border" />

</LinearLayout>
